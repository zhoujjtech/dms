server:
  port: 8080

spring:
  application:
    name: dms-liteflow

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml

    sentinel:
      transport:
        dashboard: localhost:8858
      eager: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dms_liteflow?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

  # Redis 配置（分布式缓存）
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
      cache:
        type: redis
        redis:
          time-to-live: 300000  # 5分钟过期（毫秒）
          cache-null-values: false
          key-prefix: "dms:cache:"  # 统一前缀，支持多租户
          use-key-prefix: true

# XXL-JOB 分布式调度配置
xxl:
  job:
    admin:
      addresses: http://localhost:8080/xxl-job-admin  # XXL-JOB 调度中心地址
    executor:
      appname: dms-liteflow  # 执行器应用名称
      address:  # 执行器地址（自动注册）
      ip:  # 执行器IP（自动获取）
      port: 9999  # 执行器端口
      logpath: ./xxl-job/logs  # 日志路径
      logretentiondays: 30  # 日志保留天数
    accessToken:  # 访问令牌（如果调度中心配置了）

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.dms.liteflow.infrastructure.persistence.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

liteflow:
  # 使用本地文件配置（启动时加载）
  # 实际规则由 MultiTenantFlowRuleService 动态管理
  rule-source: configData
  configData: classpath:flow/
  print-banner: true

  # 启用多租户规则服务
  multi-tenant-rule:
    enabled: true
    refresh-interval-seconds: 60

  # SQL 插件配置（已禁用，保留供参考）
  # rule-source-ext-data-map:
  #   applicationName: dms-liteflow
  #   pollingEnabled: true
  #   pollingIntervalSeconds: 60
  #   pollingStartSeconds: 60
  #   sqlLogEnabled: true
  #   chainTableName: flow_chain
  #   ... (其他配置已注释)

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.dms.liteflow: DEBUG
    com.yomahub.liteflow: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Saga 分布式事务配置
saga:
  enabled: true

# Saga 分布式事务配置
saga:
  enabled: true
  default-timeout-ms: 300000
  max-timeout-ms: 600000
  compensation-retry-count: 3
  redis-fallback-enabled: true
  data-cleanup:
    redis-retention-days: 1
    mysql-archive-days: 90
  alert:
    compensation-failure-rate-threshold: 0.05
    manual-intervention-threshold: 10
    notification-types: email,slack
